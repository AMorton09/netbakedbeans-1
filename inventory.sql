USE sakila;

CREATE TABLE inventory (
 movie_id int,
 film_id varchar(50),
 title varchar(50),
 description varchar(256),
 rental_rate varchar(50),
 category varchar(50),
 number_in_stock varchar(50),
);
